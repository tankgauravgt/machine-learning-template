# Masked Language Model (MLM) Implementation

A custom Transformer-based Masked Language Model implemented in PyTorch, utilising Hugging Face utilities for tokenisation and data collation.

## Usage Instructions
1. Install dependencies: `pip install -r requirements.txt`
2. Execute the training routine: `python train.py`
3. Perform inference using the trained model: `python inference.py`
